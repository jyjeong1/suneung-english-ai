services:
  - type: web
    name: suneung-english-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
      - key: DAILY_LIMIT
        value: "30"
